name: Build SukiSU Ultra

on:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  CCACHE_DIR: ~/.ccache
  CCACHE_MAXSIZE: 2G
  CCACHE_COMPILERCHECK: content

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 120

    steps:
      - name: Install tools
        run: |
          sudo apt-get update -y
          sudo apt-get install -y git bc bison flex libssl-dev make libelf-dev zip curl python3 lz4 zstd gcc-aarch64-linux-gnu gcc-arm-linux-gnueabihf clang-14 lld-14 llvm-14 ccache
          
      - name: Clone kernel
        run: |
          git clone --depth=1 --branch sixteen-qpr2 https://github.com/PixelOS-Devices/android_kernel_xiaomi_sm6250 kernel_source

      - name: Patch power_supply.h
        run: |
          cd kernel_source
          # Додаємо u8 arrayval[16] в структуру power_supply_propval
          # Ми шукаємо рядок 'int intval;' і після нього вставляємо 'u8 arrayval[16];'
          sed -i '/int intval;/a \    u8 arrayval[16];' include/linux/power_supply.h
          
      - name: Integrate SukiSU
        run: |
          cd kernel_source
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s susfs-main

      - name: Enable KPM
        run: |
          D="kernel_source/arch/arm64/configs/cust_defconfig"
          grep -q "CONFIG_KPM=y" "$D" || echo "CONFIG_KPM=y" >> "$D"
          grep -q "CONFIG_KALLSYMS=y" "$D" || echo "CONFIG_KALLSYMS=y" >> "$D"
          grep -q "CONFIG_KALLSYMS_ALL=y" "$D" || echo "CONFIG_KALLSYMS_ALL=y" >> "$D"
          
          # Вмикаємо BPF (ОБОВ'ЯЗКОВО ДЛЯ ANDROID 16, інакше не працюватиме мережа/Wi-Fi)
          sed -i '/CONFIG_BPF=/d' "$D"
          echo "CONFIG_BPF=y" >> "$D"
          
          # Вмикаємо BPF системні виклики
          sed -i '/CONFIG_BPF_SYSCALL=/d' "$D"
          echo "CONFIG_BPF_SYSCALL=y" >> "$D"
          
          # Примусово вмикаємо RCU підсистеми (виправляє помилки в kernel/rcu/tasks.h)
          echo "CONFIG_TASKS_RCU=y" >> "$D"
          echo "CONFIG_TASKS_TRACE_RCU=y" >> "$D"
          echo "CONFIG_TASKS_RUDE_RCU=y" >> "$D"
          
          # SUSFS конфіги
          echo "CONFIG_SUSFS=y" >> "$D"
          echo "CONFIG_SUSFS_SUS_PATH=y" >> "$D"
          echo "CONFIG_SUSFS_VNODE_MASK=y" >> "$D"
          echo "CONFIG_SUSFS_TRY_UMOUNT=y" >> "$D"
          echo "CONFIG_SUSFS_SPOOF_UNAME=y" >> "$D"
          echo "CONFIG_SUSFS_ENABLE_LOG=y" >> "$D"
          echo "CONFIG_SUSFS_HIDE_PORTS=y" >> "$D"
          
          tail -20 "$D"

      - name: Fix Kernel Config and Headers
        run: |
          cd kernel_source
          D="arch/arm64/configs/cust_defconfig"
          
          # 1. Фікс RCU помилок (вмикаємо необхідні модулі)
          echo "CONFIG_TASKS_RCU=y" >> "$D"
          echo "CONFIG_TASKS_TRACE_RCU=y" >> "$D"
          echo "CONFIG_TASKS_RUDE_RCU=y" >> "$D"
          echo "CONFIG_RCU_STALL_COMMON=y" >> "$D"
          
          # 2. Фікс Power Supply помилок (додаємо відсутні визначення прямо в код)
          # Ми додаємо #define в початок файлу, щоб компілятор не сварився на "undeclared identifier"
          FILE="drivers/power/supply/power_supply_sysfs.c"
          sed -i '1i #define POWER_SUPPLY_PROP_ROMID 100\n#define POWER_SUPPLY_PROP_DS_STATUS 101\n#define POWER_SUPPLY_PROP_PAGE0_DATA 102\n#define POWER_SUPPLY_PROP_PAGE1_DATA 103\n#define POWER_SUPPLY_PROP_PAGEDATA 104\n#define POWER_SUPPLY_PROP_VERIFY_MODEL_NAME 105' "$FILE"
          
      - name: Reset and Optimize Cache
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ccache-kernel-sm6250-${{ github.run_id }}
          restore-keys: |
            ccache-kernel-sm6250-

      - name: Apply Bulletproof Patches
        run: |
          cd kernel_source
          
          # 1. Фікс асемблера для SukiSU
          if [ -f arch/arm64/kernel/entry.S ]; then sed -i 's/.*ValidContext.*/ValidContext:/g' arch/arm64/kernel/entry.S; fi
          
          # 2. Видаляємо помилковий артефакт ValidContext з C-файлу, якщо він туди проліз
          if [ -f arch/arm64/kernel/sys.c ]; then sed -i 's/.*ValidContext.*//g' arch/arm64/kernel/sys.c; fi
          
          # 3. ВИДАЛЯЄМО ГАНЕБНИЙ БАГ XIAOMI (Unsupported NR_CPUS) З СИРЦІВ
          if [ -f include/trace/events/sched.h ]; then sed -i '/Unsupported NR_CPUS/d' include/trace/events/sched.h; fi
          
          # 4. Фікс сумісності драйвера dm-user з Android 14+
          if [ -f drivers/md/dm-user.c ]; then sed -i 's/BIO_MAX_PAGES/256/g' drivers/md/dm-user.c; fi
          
          # 5. Примусове лінкування файлів маскування SUSFS (запобігає undefined symbol)
          if [ -f fs/Makefile ]; then grep -q "susfs.o" fs/Makefile || echo "obj-y += susfs.o" >> fs/Makefile; fi

      - name: Build
        run: |
          cd kernel_source
          make O=out ARCH=arm64 cust_defconfig

          ccache -z
          
          # 4.5) Фікс для Power Supply (застарілі заголовки та збереження швидкої зарядки 33W)
          FILE_PS="drivers/power/supply/power_supply_sysfs.c"
          if [ -f "$FILE_PS" ]; then
            sed -i '1i #define POWER_SUPPLY_PROP_ROMID 100\n#define POWER_SUPPLY_PROP_DS_STATUS 101\n#define POWER_SUPPLY_PROP_PAGE0_DATA 102\n#define POWER_SUPPLY_PROP_PAGE1_DATA 103\n#define POWER_SUPPLY_PROP_PAGEDATA 104\n#define POWER_SUPPLY_PROP_VERIFY_MODEL_NAME 105' "$FILE_PS"
            echo "✓ Додано дефайни констант у power_supply_sysfs.c"
          fi

          if [ -f include/linux/power_supply.h ]; then
            sed -i '/int intval;/a \    u8 arrayval[16];' include/linux/power_supply.h
            echo "✓ Додано підтримку arrayval у power_supply.h"
          fi
          
          # Запуск компіляції. CC тепер абсолютно чистий, без жодних макросів!
          make -j$(nproc --all) O=out ARCH=arm64 \
            CC="ccache clang-14" \
            LD=ld.lld-14 \
            AR=llvm-ar-14 \
            NM=llvm-nm-14 \
            OBJCOPY=llvm-objcopy-14 \
            OBJDUMP=llvm-objdump-14 \
            STRIP=llvm-strip-14 \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabihf- \
            LLVM=1 \
            LLVM_IAS=0 \
            KCFLAGS="-Dcompat_uptr_t=__u32 -Wno-implicit-function-declaration -Wno-error" \
            AFLAGS="-Wa,-W" \
            Image dtbs 2>&1 | tee ../build.log || true
            
          ccache -s
          
          echo "=== Перевірка помилок ==="
          grep -i "error:" ../build.log | head -30 || true
          
          echo "=== Перевірка вихідних файлів ==="
          ls -lh out/arch/arm64/boot/ || true

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ github.run_number }}
          path: build.log
          retention-days: 3

      - name: Package
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3 AnyKernel3
          sed -i 's/do.devicecheck=1/do.devicecheck=0/' AnyKernel3/anykernel.sh

          if [ -f kernel_source/out/arch/arm64/boot/Image ]; then
            gzip -c9 kernel_source/out/arch/arm64/boot/Image > AnyKernel3/Image.gz
          elif [ -f kernel_source/out/arch/arm64/boot/Image.gz ]; then
            cp kernel_source/out/arch/arm64/boot/Image.gz AnyKernel3/
          elif [ -f kernel_source/out/arch/arm64/boot/Image-dtb ]; then
            gzip -c9 kernel_source/out/arch/arm64/boot/Image-dtb > AnyKernel3/Image.gz
          else
            echo "❌ КРИТИЧНА ПОМИЛКА: жодного файлу ядра не знайдено!"
            exit 1
          fi

          find kernel_source/out/arch/arm64/boot/dts -name "*.dtb" -exec cat {} >> AnyKernel3/dtb \; 2>/dev/null || true

          cd AnyKernel3
          zip -r9 "../SukiSU-joyeuse.zip" . -x ".git*"
          cd ..

      - name: Upload zip
        uses: actions/upload-artifact@v4
        with:
          name: SukiSU-joyeuse-${{ github.run_number }}
          path: ./*.zip
          retention-days: 5
